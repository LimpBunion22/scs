task_id = "DESKTOPSMOKE-001"
task_name = "DESKTOPSMOKE-001 - Manual desktop smoke and issue triage"
agent_type = "worker"
model = "gpt-5.4"
model_reasoning_effort = "medium"
status = "completed"
agent_id = "019f1018-89b8-7762-a87b-c2c55051a2fe"
agent_nickname = "Dirac"
completion_artifact = "docs/contracts/desktop_smoke_checklist.md"
blocked_by = []
launch_order = 1
notes = [
  "Run first in the desktop validation/refinement round.",
  "Validation/documentation task; do not implement fixes.",
]

prompt = '''
You are working in /home/vitiate/Desktop/Hetairoi/scs.

Read first:
- AGENTS.md
- docs/contracts/agent_task_contracts.md
- docs/contracts/agent_parallelization.md
- docs/decisions/0001_vertical_slice_sequence.md

Your assigned task is:
DESKTOPSMOKE-001

Follow the exact task contract from docs/contracts/agent_task_contracts.md.

Before editing:
1. Confirm the relevant files and allowed files from the contract.
2. Inspect only the required context, relevant source files, and related tests.
3. Do not modify files outside the allowed list unless the task becomes impossible without doing so; if that happens, stop and report the pending decision.

Implementation rules:
- Preserve deterministic simulation behavior.
- Do not add UI, rendering, dependencies, or speculative systems unless the contract explicitly allows it.
- Keep changes small and scoped.
- Add or update tests required by the acceptance criteria.
- Update only the docs required by the deliverables.

Verification:
- Run the relevant build and tests.
- At minimum, run:
  cmake -S . -B build
  cmake --build build
  ctest --test-dir build --output-on-failure

Return the completion report in the AGENTS.md format:
Completed:
Files changed:
Tests added or updated:
Tests executed:
Assumptions:
Pending decisions:
Risks:
Suggested next task:
'''
